namespace Nabu.Services
{
    /*
    public interface ISettingsService
    {
        Task Commit(IEmulatorSettings settings);

        Task<IEmulatorSettings> CurrentAsync();
        IEmulatorSettings Current();
    }
    */
}